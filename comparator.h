#ifndef _COMPARATOR_H_
#define _COMPARATOR_H_

// given two histograms of size nBins returns the probability that they are the same
double intersection(double* hist1, double* hist2, int nBins);

#endif